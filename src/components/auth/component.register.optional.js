import React from 'react';

import './component.register.css';

const RegisterOptionalInfo = ({
    children,
    inverted,
    ...otherProps
}) => (
    <div>OPTIONAL THINGS</div>
       
);

export default RegisterOptionalInfo;
